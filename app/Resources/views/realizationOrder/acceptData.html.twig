{% extends 'base.html.twig' %}

{% block body %}
    {% if is_granted('ROLE_USER') %}
		{{ app.user.username}}<br>
		{{ app.user.name}}
		{{ app.user.city}}
		{{ app.user.street}}
	{% else %}
		Dane usera niezalogowanego
	{% endif %}
	<td><a href="{{ path('history')}}">REALIZUJ</a></td>
{% endblock %}