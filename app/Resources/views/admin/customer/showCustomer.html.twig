{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col s12">
                {% for user in allUsers %}
					{{ user.name }}
				{% endfor %}
            </div>
        </div>
    </div>
{% endblock %}