{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col s12">
                {% for user in allUsers %}
					{{ user.name }}
					<a href=" {{ path('editCustomerData', {id:user.id}) }} ">Edytuj</a>
					<a href="">Usuń</a><br>
				{% endfor %}
            </div>
        </div>
    </div>
{% endblock %}